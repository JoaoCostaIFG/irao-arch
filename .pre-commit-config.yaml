repos:
  - repo: local
    hooks:
      - id: sort-depends
        name: Sort depends array in PKGBUILDs
        entry: scripts/sort-depends.sh
        language: script
        files: ^packages/.*/PKGBUILD$
